additionalProperties:
  formFields:
    - default: "auto"
      envKey: REGISTRY
    #  labelEn: REGISTRY
    #  labelZh: 镜像源模式(默认即可)
      label:
        zh: 镜像源模式(默认即可)
        en: Registry mode (default is fine)
      required: true
      type: string