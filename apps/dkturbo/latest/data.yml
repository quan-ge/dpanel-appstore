additionalProperties:
  formFields:
    - default: "auto"
   #   envKey: REGISTRY
    #  labelEn: REGISTRY
      label:
        zh: 镜像源模式(默认即可)
        en: Registry mode (default is fine)
      labelZh: 镜像源模式(默认即可)
      required: true
      type: string