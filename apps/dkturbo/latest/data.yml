additionalProperties:
  formFields:
    - default: "auto"
      envKey: REGISTRY
      label:
        zh: 镜像源模式(默认即可)
        en: Registry mode (default is fine)
      required: true
      type: string